using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HomeScreenScript : MonoBehaviour
{

    public List<GameObject> listOfButtons = new List<GameObject>();
    public gameObject OrdersButton;
    public gameObject GPSButton;
    public gameObject AlertsButton;
    public gameObject TBDButton;
    public gameObject CurrentRatingButton;

    private int currentActiveButtonIndex = 0;

    void handleHomeScreenGamepadNavigation()
    {

    }

    // Start is called before the first frame update
    void Start()
    {

        listOfButtons.Add(GameO)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
